{% extends "base.html" %}

{% block content %}
	<ul>
		<!{% for entry in entries %}>
			<li><h2>{{ entry.title }}</h2>
			<h5>{{ entry.description }}</h3>	
			{% if  entry.image  %}
				<img src="{{url_for('static', filename='files/'+entry.image)}}" class="img-polaroid">
			{% endif %}
			{% else %}	
				<li><em>No existen anuncios</em>
		<!{% endfor %}>
  </ul>

{% endblock content %}
