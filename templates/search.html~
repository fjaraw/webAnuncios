{% extends "base.html" %}

{% block content %}
  <form class="form-signin" name="login" action="{{url_for('search')}}" method="post">

  <h2><label for="title" class="form-group">Busqueda</label></h2>
        <div class="col-sm-10">
		<FORM method=GET action="http://www.google.es/search" target="_blank">
        	<input class="form-control" type="text" name="busq" placeholder="¿Qué desea buscar?">
        </div>
  <input type="submit" value="Buscar" class="btn btn-default">
  <h3 class="form-signin-heading">Buscar por</h3>
  <div class="col-sm-5">
	<h3 type="text"><a href="{{url_for('arriendo')}}">Arriendo</a></h3>
   	<h3 type="text"><a href="{{url_for('compra')}}">Compra</a></h3>
    	<h3 type="text"><a href="{{url_for('venta')}}">Venta</a></h3>
    	<h3 type="text"><a href="{{url_for('servicio')}}">Servicio</a></h3>
  </div>
  </form>

{% endblock content %}
